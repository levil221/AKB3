#include "Console.h"



void Console::print(std::string String)
{
	//--------PRINTS

	/*void printSequenceWihtMotiv()
	{
	vector<int> startes;
	SequenceFragment *temp;
	SetConsoleTextAttribute(hConsole, 15);

	for (auto consensus : seqences) {
	cout << consensus.seqID << endl;
	for (auto id: maxClinque.list) {
	if (vertexes[id]->seqId == consensus.seqID) {
	startes.push_back(vertexes[id]->begin);
	}
	}
	for (int i = 0; i < consensus.consensus.size(); i++) {
	for (auto nn : startes) {
	if (i == nn) {
	SetConsoleTextAttribute(hConsole, 2);
	for (int j = 0; j < window; j++, i++) {
	cout << consensus.consensus[i];
	}
	SetConsoleTextAttribute(hConsole, 15);
	}
	}
	cout << consensus.consensus[i];
	}
	cout << endl<< endl;
	startes.clear();
	}
	}*/
}

Console::Console()
{
}


Console::~Console()
{
}
